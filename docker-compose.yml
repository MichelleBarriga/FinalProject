services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    image: movies-frontend:local
    ports:
      - "8080:80"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    networks:
      - webnet

networks:
  webnet:
    driver: bridge
